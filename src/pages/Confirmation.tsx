import { Link, useLocation } from "react-router-dom";
export default function Confirmation(){const q=new URLSearchParams(useLocation().search);const kind=q.get("kind");const ref=q.get("ref");const copy=kind==="suggestion"?["Your campsite suggestion has been received","We’ll review the place before it is added to Campin."]:kind==="host"?["Your campsite submission has been received","We’ll review the property details and contact you about next steps."]:["You’re on the Campin waitlist","We’ll send useful campsite and community updates when they match your interests."];return <main className="min-h-screen bg-offwhite px-4 pt-32 text-center"><section className="mx-auto max-w-xl rounded-3xl bg-white p-9 shadow-sm"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-forest text-2xl text-white">✓</div><h1 className="mt-6 text-3xl font-extrabold text-forest">{copy[0]}</h1><p className="mt-3 leading-7 text-textgrey">{copy[1]}</p>{ref&&<p className="mt-6 rounded-xl bg-offwhite p-3 text-sm font-bold text-forest">Reference: {ref}</p>}<Link to="/explore" className="mt-7 inline-flex rounded-xl bg-orange px-5 py-3 font-bold text-white">Explore campsites</Link></section></main>}
