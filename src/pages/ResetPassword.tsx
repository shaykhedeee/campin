import { useState, type FormEvent } from "react";
import { Link } from "react-router-dom";
import { supabase } from "../lib/adminAuth";
export default function ResetPassword() { const [email,setEmail]=useState(""); const [message,setMessage]=useState(""); const submit=async(e:FormEvent)=>{e.preventDefault();if(!supabase)return setMessage("Password reset is being connected. Please try again shortly."); const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/reset-password`});setMessage(error?error.message:"If that account exists, a reset link is on its way.");}; return <main className="min-h-screen bg-offwhite px-4 pt-28"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl bg-white p-8 shadow-sm"><h1 className="text-3xl font-extrabold text-forest">Reset your password</h1><p className="mt-2 text-sm leading-6 text-textgrey">Enter the email used for your Campin account.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-6 h-12 w-full rounded-xl border border-forest/15 bg-offwhite px-3 outline-none focus:border-orange" placeholder="you@example.com"/><button className="mt-4 w-full rounded-xl bg-forest py-3 font-bold text-white">Send reset link</button>{message&&<p role="status" className="mt-4 text-sm text-forest">{message}</p>}<Link to="/auth" className="mt-5 block text-sm font-bold text-orange">Back to sign in</Link></form></main>; }
