import { Link } from "react-router-dom";
import { useAuth } from "../lib/auth";
export default function Account() { const { user, loading, signOut } = useAuth(); if (loading) return <main className="min-h-screen bg-offwhite pt-32 text-center text-forest">Loading account…</main>; if (!user) return <main className="min-h-screen bg-offwhite pt-32 text-center"><p className="text-forest">Sign in to manage your Campin account.</p><Link className="mt-5 inline-flex rounded-xl bg-forest px-5 py-3 font-bold text-white" to="/auth">Sign in</Link></main>; return <main className="min-h-screen bg-offwhite px-4 pt-28 pb-16"><section className="mx-auto max-w-2xl rounded-3xl bg-white p-8 shadow-sm"><p className="text-sm font-bold text-orange">Your Campin account</p><h1 className="mt-2 text-3xl font-extrabold text-forest">{user.user_metadata.full_name || user.email}</h1><p className="mt-2 text-textgrey">{user.email}</p><p className="mt-7 rounded-xl bg-offwhite p-4 text-sm leading-6 text-textgrey">Your campsite enquiries and saved stays will appear here as Campin’s verified inventory comes online.</p><button type="button" onClick={()=>void signOut()} className="mt-6 rounded-xl border border-forest/15 px-5 py-3 font-bold text-forest hover:bg-offwhite">Sign out</button></section></main>; }
