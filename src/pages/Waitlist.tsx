import { useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { submitMvpLead } from "../lib/mvpLeadStore";
import { useLeadSubmission } from "../components/leads/useLeadSubmission";
export default function Waitlist() { const navigate=useNavigate(); const sub=useLeadSubmission(); const [form,setForm]=useState({name:"",email:"",phone:"",interests:"",newsletter:false,consent:false}); const submit=async(e:FormEvent)=>{e.preventDefault();const result=await sub.run(()=>submitMvpLead({type:"camper_waitlist",sourcePage:"/waitlist",name:form.name,email:form.email,phone:form.phone,status:"waitlist",consent:form.consent,payload:{destinationInterests:form.interests,newsletter:form.newsletter}}));if(result?.remote==="synced")navigate(`/confirmation?kind=waitlist&ref=${encodeURIComponent(result.lead.id)}`);};return <main className="min-h-screen bg-offwhite px-4 pt-28 pb-20"><div className="mx-auto grid max-w-5xl gap-10 lg:grid-cols-[1fr_1.1fr]"><section><p className="font-bold text-orange">Campin waitlist</p><h1 className="mt-3 font-serif text-5xl font-bold leading-none text-forest">Get campsite updates that match your next trip.</h1><p className="mt-6 text-lg leading-8 text-textgrey">Tell us where you want to camp. We’ll share new campsite listings and practical guides when they are useful.</p></section><form onSubmit={submit} className="rounded-3xl bg-white p-7 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold text-forest">Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-2 h-12 w-full rounded-xl border border-forest/15 bg-offwhite px-3"/></label><label className="text-sm font-bold text-forest">Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="mt-2 h-12 w-full rounded-xl border border-forest/15 bg-offwhite px-3"/></label></div><label className="mt-4 block text-sm font-bold text-forest">WhatsApp number <span className="font-medium text-textgrey">optional</span><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} className="mt-2 h-12 w-full rounded-xl border border-forest/15 bg-offwhite px-3"/></label><label className="mt-4 block text-sm font-bold text-forest">Places you want to explore<textarea value={form.interests} onChange={e=>setForm({...form,interests:e.target.value})} rows={3} className="mt-2 w-full rounded-xl border border-forest/15 bg-offwhite p-3" placeholder="For example: Coorg, campervan sites near Bangalore"/></label><label className="mt-3 flex gap-3 text-sm text-textgrey"><input type="checkbox" checked={form.newsletter} onChange={e=>setForm({...form,newsletter:e.target.checked})}/>Send me useful campsite and guide updates.</label><label className="mt-3 flex gap-3 text-sm text-textgrey"><input required type="checkbox" checked={form.consent} onChange={e=>setForm({...form,consent:e.target.checked})}/>I agree that Campin can respond to this request.</label><button disabled={sub.isSaving} className="mt-6 w-full rounded-xl bg-orange py-3 font-bold text-white">{sub.isSaving?"Saving…":"Join waitlist"}</button><p role="status" className="mt-3 text-sm text-forest">{sub.message}</p></form></div></main>; }
