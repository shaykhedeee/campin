import { useEffect, useState, type FormEvent } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { ExternalLink, MapPin, Share2 } from 'lucide-react';
import { PropertyImage } from '../components/CampsiteCard';
import type { Campsite } from '../lib/catalogue';
import { useAuth } from '../lib/auth';
import { createMarketplaceEnquiry, getMarketplaceWhatsapp } from '../lib/marketplaceApi';
export default function MarketplaceListing(){
 const {id=''}=useParams(); const [site,setSite]=useState<Campsite|null>(null);const [loading,setLoading]=useState(true);const [message,setMessage]=useState(''); const [saved,setSaved]=useState<{id:string;reference:string;handoffReady:boolean}|null>(null); const {user}=useAuth();const nav=useNavigate();
 const [form,setForm]=useState({arrive:'',depart:'',guests:'2',style:'hosted_tent',vehicle:'',questions:'',consent:false});
 useEffect(()=>{fetch(`/api/search?slug=${encodeURIComponent(id)}`).then(r=>r.json()).then(d=>setSite(d.campsites?.[0]||null)).catch(()=>setSite(null)).finally(()=>setLoading(false));},[id]);
 const submit=async(e:FormEvent)=>{e.preventDefault();if(!user){sessionStorage.setItem('campin.tripDraft',JSON.stringify(form));nav(`/auth?next=${encodeURIComponent(`/listing/${id}`)}`);return;}if(!site||!form.consent)return setMessage('Please confirm that Campin can share this request with the operator.');try{const result=await createMarketplaceEnquiry({listingId:site.id,startDate:form.arrive,endDate:form.depart,guests:Number(form.guests),campingStyle:form.style,vehicleDetails:form.vehicle,questions:form.questions});setSaved(result);setMessage(result.handoffReady?'Your request is saved. Review the message before opening WhatsApp.':'Your request is saved. Campin needs to confirm an approved WhatsApp contact; use the official operator page meanwhile.');}catch(error){setMessage(error instanceof Error?error.message:'We could not save this request.');}};
 const handoff=async()=>{if(!saved)return;try{const d=await getMarketplaceWhatsapp(saved.id);await navigator.clipboard?.writeText(d.message);window.open(d.whatsappUrl,'_blank','noopener');}catch{setMessage('The host contact is not yet approved for Campin. Please use the official operator page below.');}};
 if(loading)return <main className="min-h-screen bg-offwhite pt-32 text-center">Loading campsite…</main>;if(!site)return <main className="min-h-screen bg-offwhite pt-32 text-center"><h1 className="text-2xl font-bold">This campsite is unavailable</h1><Link className="mt-4 inline-block underline" to="/explore">Explore campsites</Link></main>;
 return <main className="min-h-screen bg-offwhite px-4 pb-28 pt-28 text-forest"><div className="mx-auto grid max-w-7xl gap-9 lg:grid-cols-[1.55fr_.75fr]"><section><div className="aspect-[16/9] overflow-hidden rounded-3xl"><PropertyImage site={site} className="h-full w-full object-cover"/></div><div className="mt-7 flex items-start justify-between gap-4"><div><p className="flex items-center gap-1 text-sm text-textgrey"><MapPin size={15}/>{site.destination}, {site.state}</p><h1 className="mt-2 font-serif text-4xl font-bold">{site.title}</h1><p className="mt-3 text-lg leading-8 text-textgrey">{site.description}</p></div><button onClick={()=>navigator.share?.({title:site.title,url:location.href})} aria-label="Share campsite"><Share2/></button></div><div className="mt-7 rounded-2xl border border-forest/10 bg-white p-6"><h2 className="text-xl font-bold">What Campin has checked</h2><p className="mt-2 text-sm leading-6 text-textgrey">This is a source-reviewed listing based on the operator’s public page, checked {new Date(site.checkedAt).toLocaleDateString('en-IN')}. It is not a Campin host partnership or live availability confirmation.</p><div className="mt-5 flex flex-wrap gap-2">{site.amenities.map(a=><span className="rounded-full bg-offwhite px-3 py-2 text-sm" key={a}>{a}</span>)}</div><a href={site.sourceUrl} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 font-bold text-orange underline">Open official operator page <ExternalLink size={15}/></a></div></section><aside className="h-max rounded-3xl bg-white p-6 shadow-lg lg:sticky lg:top-28"><p className="font-bold">{site.price?`From ₹${site.price.toLocaleString('en-IN')} / ${site.priceUnit}`:'Ask operator for pricing'}</p><p className="mt-1 text-xs text-textgrey">Host confirmation required</p><form onSubmit={submit} className="mt-5 space-y-4"><div className="grid grid-cols-2 gap-3"><label className="text-sm font-bold">Arrival<input required type="date" value={form.arrive} onChange={e=>setForm({...form,arrive:e.target.value})} className="mt-1 h-11 w-full rounded-lg bg-offwhite px-2"/></label><label className="text-sm font-bold">Departure<input required type="date" value={form.depart} onChange={e=>setForm({...form,depart:e.target.value})} className="mt-1 h-11 w-full rounded-lg bg-offwhite px-2"/></label></div><label className="block text-sm font-bold">Guests<input required type="number" min="1" max={site.maxGuests||50} value={form.guests} onChange={e=>setForm({...form,guests:e.target.value})} className="mt-1 h-11 w-full rounded-lg bg-offwhite px-3"/></label><label className="block text-sm font-bold">Camping style<select value={form.style} onChange={e=>setForm({...form,style:e.target.value})} className="mt-1 h-11 w-full rounded-lg bg-offwhite px-3"><option value="hosted_tent">Hosted stay</option><option value="own_tent">Own tent</option><option value="campervan">Campervan / RV</option></select></label><label className="block text-sm font-bold">Vehicle or questions <textarea value={form.questions} onChange={e=>setForm({...form,questions:e.target.value})} className="mt-1 w-full rounded-lg bg-offwhite p-3" rows={3}/></label><label className="flex gap-2 text-xs"><input required type="checkbox" checked={form.consent} onChange={e=>setForm({...form,consent:e.target.checked})}/>I agree Campin can save this enquiry and pass it to an approved operator contact.</label><button className="w-full rounded-xl bg-orange px-4 py-3 font-bold text-white">{user?'Check availability':'Sign in to check availability'}</button></form>{saved?.handoffReady&&<button onClick={handoff} className="mt-3 w-full rounded-xl bg-forest px-4 py-3 font-bold text-white">Continue on WhatsApp</button>}{message&&<p role="status" className="mt-4 text-sm leading-6">{message}</p>}</aside></div></main>;
}
